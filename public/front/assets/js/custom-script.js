// Pre view video startfunction show_prev(link, thumbnail=null){    $('#video_src').attr('src',link)    $('#video_src').attr('type', 'video/mp4')    var myVideo = document.getElementById("video1");     // if (myVideo.paused)     myVideo.load(); //   else     // myVideo.pause(); }// preview video pause(function ($) {    $(document).on('hidden.bs.modal', function (e) {      var video = document.getElementById("video1");       video.pause();      video.currentTime = 0;    });  })(jQuery);  function videoEnd(){    document.getElementById("close").click();  }  var loginForm = $("#loginForm");loginForm.submit(function(e){    e.preventDefault();    var formData = loginForm.serialize();    $.ajax({        url:'/auth/signin',        type:'POST',        data:formData,        success:function(data){            if(data.auth){              window.location.href =data.intended;            }else{              alert('Your login id or password is wrong.');            };        },        error: function (data) {           console.log(data);            alert('Your login id or password is wrong.');          //  }        }    });});